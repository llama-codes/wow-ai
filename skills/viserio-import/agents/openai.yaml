interface:
  display_name: "Viserio Import Workflow"
  short_description: "Orchestrate Viserio assignment workflows"
  default_prompt: "Use $viserio-import to orchestrate a Viserio export, Markdown plan, import JSON, and validation workflow."
